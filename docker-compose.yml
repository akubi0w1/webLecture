version: "3.7"
services:
  app:
    build: ./django
    volumes:
      - ./django/src:/code
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
  
  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
